# StudyPilot recording plan

Target length: about 4 minutes 15 seconds, below the five-minute limit. Public YouTube/Vimeo upload is still pending.

## Recording prerequisites

The Qwen endpoint is stopped at the owner's request. Resume only with explicit approval, warm it, run the real workflow smoke check, then record a fresh run. Do not start inference for rehearsal.

Use a fictional input and a temporary database. Hide private tabs, credentials and account information. Open Connection details to show configuration, but do not call that a successful model test. For a scripted rehearsal, keep "Scripted responses — no model inference" visible in the recording; do not present that clip as new live-model evidence.

## Pitch

StudyPilot turns coursework and available hours into a study week you can review. It keeps the source deadlines visible, protects blocked time and asks before adding local calendar events.

## Screen sequence

| Time | Screen/action | Narration cue |
| --- | --- | --- |
| 0:00–0:25 | Show the planner intake. | A student needs to fit several deadlines around fixed commitments. The task is deciding what fits, not generating another task list. |
| 0:25–1:05 | Enter synthetic coursework, including a TBA date, and protected time. | Show the expected structured format and the available hours. Do not imply that PDF import exists. |
| 1:05–1:50 | Build the week, inspect cited items and conflict notices. | Explain what Strands advised and what the scheduler checked. Keep unresolved dates visible. |
| 1:50–2:25 | Inspect the exact session list, approve, and download ICS. | Show the saved calendar result. This is local storage and export, not Google Calendar sync. |
| 2:25–3:25 | Revise inputs as a separate copy, then reopen the original. | Show fresh approval and explain that approving a copy adds separate events. The original is not silently replaced. |
| 3:25–4:10 | Show current architecture and the source of the two read-only tools. | Explain Strands, the configured model, deterministic constraints and approval. Finish with one limitation and the usable exported calendar. |

## Final checks

- Show a useful output and one difficult case, not only a landing page.
- Name Strands Agents SDK and the provider used in the captured run. Use architecture-current.png.
- Distinguish source evidence, model advice, deterministic validation and human approval.
- Structured coursework input only; no PDF parser, background LMS monitoring, or external calendar synchronization.
- Disclose Codex as a development assistant; do not invent user adoption, results or deployments.
- Watch the entire uploaded video while signed out. Confirm public playback, readable text, intelligible audio and a duration under five minutes.
- Copy the verified public video URL into the submission. Recording a file alone does not complete this requirement.
